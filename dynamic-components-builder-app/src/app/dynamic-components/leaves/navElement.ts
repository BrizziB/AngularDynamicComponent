export class NavElement {
  id: string;
  value: string;

  constructor(id: string, val: string) {
    this.id = id;
    this.value = val;
  }
  // qui poi potrebbe aver senso metterci un modo per collegare i tab con le pagine mostrate!

}
